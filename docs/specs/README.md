# Spec Index

> Slice의 "무엇을 만들 것인가"를 정의한다. Plan(어떻게)과는 분리한다 — [../plans/](../plans/).
> 파일 위치: `docs/specs/{slice}.md`. Milestone 폴더로 나누지 않는다 — Milestone은 GitHub 쪽 그룹핑이고, 파일 경로는 그와 독립적으로 유지한다.

---

## 작성 시점

workflow.md 4단계(Specify). Plan보다 먼저 쓴다.

## 형식

```markdown
# {slice} Spec

## 입력/출력
무엇을 받아서 무엇을 낸다

## Acceptance Criteria
- 정입력 케이스
- 오류 케이스
```

## GitHub Issue 미러

저장소(이 폴더의 markdown)가 SSOT. Slice Issue 본문에는 요약 + 이 파일 링크만 둔다. 전문 복사 안 함.

---

## 목록

| Slice | Spec |
|-------|------|
| {{slice명}} | [{{slice}}.md](./{{slice}}.md) |
